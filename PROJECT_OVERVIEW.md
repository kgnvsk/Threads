# 📂 Огляд проєкту Threads Auto-Publisher Bot

## 🎯 Що це?

Автоматизований Telegram бот, який моніторить ваш Telegram канал та автоматично публікує всі нові пости в Threads (Meta).

## ✨ Ключові можливості

### 1. Автоматична публікація
- 🤖 Бот слухає ваш Telegram канал 24/7
- 🚀 Кожен новий пост автоматично публікується в Threads
- ⚡ Миттєва реакція на нові пости

### 2. Розумна обробка контенту

**Короткі тексти (до 450 символів):**
```
Telegram: "Привіт! Це короткий пост."
         ↓
Threads:  "Привіт! Це короткий пост."
```

**Довгі тексти (понад 450 символів):**
```
Telegram: [Текст 800 символів]
         ↓
Threads:  Пост 1: "Перша частина... ⬇️"
          └→ Відповідь 1: "/1 Друга частина... ⬇️"
             └→ Відповідь 2: "/2 Третя частина"
```

**Фото з текстом:**
```
Telegram: 🖼️ + [довгий текст]
         ↓
Threads:  Пост: 🖼️ + "Перша частина... ⬇️"
          └→ Відповідь: "/1 Друга частина"
```

**Медіа-групи:**
```
Telegram: 🖼️🖼️🖼️ + текст
         ↓
Threads:  Карусель: 🎠 [3 фото] + текст
```

### 3. Підтримка всіх типів контенту
- ✅ Текстові пости
- ✅ Фото (JPG, PNG)
- ✅ Відео (MP4, MOV до 5 хв)
- ✅ Медіа-групи (2-10 елементів)
- ✅ Змішаний контент (фото/відео + текст)

### 4. Розумна розбивка текстів
- 🧠 Використовує GPT для природної розбивки
- 📝 Не обриває речення посередині
- 🎯 Зберігає логіку та форматування
- ⬇️ Додає навігаційні символи

## 📁 Структура проєкту

```
/Users/admin/Threads/
│
├── 🤖 ОСНОВНІ ФАЙЛИ
│   ├── bot.py              ← Головний файл бота
│   ├── config.py           ← Конфігурація (РЕДАГУЙТЕ ЦЬО)
│   ├── threads_api.py      ← Клас для роботи з Threads API
│   ├── text_splitter.py    ← Розбивка довгих текстів через GPT
│   └── test_connection.py  ← Скрипт для тестування налаштувань
│
├── 📚 ДОКУМЕНТАЦІЯ
│   ├── README.md           ← Основна документація
│   ├── QUICKSTART.md       ← Швидкий старт (5 хвилин)
│   ├── SETUP_GUIDE.md      ← Детальна інструкція з налаштування
│   ├── API_EXAMPLES.md     ← Приклади роботи з Threads API
│   └── PROJECT_OVERVIEW.md ← Цей файл
│
├── ⚙️ КОНФІГУРАЦІЯ
│   ├── requirements.txt    ← Python залежності
│   ├── env.example         ← Приклад змінних середовища
│   └── .gitignore          ← Git ignore файл
│
└── 📊 ЛОГИ (після запуску)
    └── threads_bot.log     ← Логи роботи бота
```

## 🔧 Архітектура

### Основні компоненти

```
┌─────────────────┐
│ Telegram канал  │
└────────┬────────┘
         │ Нові пости
         ↓
┌─────────────────┐
│   bot.py        │ ← Telegram бот (слухає канал)
└────────┬────────┘
         │
         ├→ Текст довгий?
         │      ↓
         │  ┌──────────────┐
         │  │text_splitter │ ← GPT розбиває текст
         │  └──────────────┘
         │
         └→ Публікувати
                ↓
         ┌──────────────┐
         │threads_api.py│ ← Взаємодія з Threads API
         └──────┬───────┘
                │
                ↓
         ┌──────────────┐
         │   Threads    │ ← Опубліковано!
         └──────────────┘
```

### Потік обробки поста

```
1. Telegram → [Новий пост]
                ↓
2. Bot → [Аналіз типу контенту]
                ↓
        ┌───────┴───────┐
        │               │
   [Текст]        [Медіа]
        │               │
        ↓               ↓
   [Довгий?]      [Група?]
        │               │
   [GPT розбиває]  [Створює карусель]
        │               │
        └───────┬───────┘
                ↓
3. Threads API → [Створює media container]
                ↓
4. Threads API → [Перевіряє статус]
                ↓
5. Threads API → [Публікує]
                ↓
6. Якщо є ще частини → [Створює replies]
                ↓
        ✅ Готово!
```

## 🔌 Інтеграції

### 1. Telegram Bot API
- **Бібліотека:** `python-telegram-bot`
- **Версія:** 21.0
- **Функція:** Моніторинг каналу та отримання контенту

### 2. Threads API (Meta Graph API)
- **Endpoint:** `https://graph.threads.net/v1.0`
- **Бібліотека:** `requests`
- **Функція:** Публікація контенту в Threads

### 3. OpenAI API
- **Модель:** `gpt-4o-mini` (за замовчуванням)
- **Бібліотека:** `openai`
- **Функція:** Розумна розбивка довгих текстів

## 🚀 Використання

### Швидкий старт

```bash
# 1. Встановлення
pip install -r requirements.txt

# 2. Налаштування (редагуйте config.py)
nano config.py

# 3. Тестування
python test_connection.py

# 4. Запуск
python bot.py
```

### Production запуск

**Через systemd (Linux):**
```bash
sudo systemctl enable threads-bot
sudo systemctl start threads-bot
```

**Через Docker:**
```bash
docker build -t threads-bot .
docker run -d --restart unless-stopped threads-bot
```

**Через nohup (Mac/Linux):**
```bash
nohup python bot.py > bot.log 2>&1 &
```

## 📊 Моніторинг та логування

### Рівні логування

```python
INFO  - Нормальна робота
WARNING - Попередження (не критичні)
ERROR - Помилки (з деталями)
DEBUG - Детальна інформація для відлагодження
```

### Приклад логів

```
2025-01-14 12:00:00 - __main__ - INFO - Запуск Threads Bot...
2025-01-14 12:00:01 - __main__ - INFO - Бот готовий до роботи!
2025-01-14 12:05:30 - __main__ - INFO - Отримано новий пост: 12345
2025-01-14 12:05:30 - __main__ - INFO - Обробляємо текстовий пост (567 символів)
2025-01-14 12:05:30 - text_splitter - INFO - Текст довгий, розбиваємо
2025-01-14 12:05:35 - text_splitter - INFO - Текст розбито на 3 частини
2025-01-14 12:05:40 - threads_api - INFO - Пост створено: 1234567890
2025-01-14 12:05:45 - threads_api - INFO - Пост опубліковано: 9876543210
2025-01-14 12:05:50 - __main__ - INFO - ✅ Успішно! (3 частини)
```

### Переглядати логи в реальному часі

```bash
tail -f threads_bot.log
```

## 🔒 Безпека

### Що НЕ треба робити
- ❌ Додавати `config.py` з реальними токенами в Git
- ❌ Викладати токени в публічні репозиторії
- ❌ Діліться access tokens з іншими

### Що ТРЕБА робити
- ✅ Використовувати змінні середовища для токенів
- ✅ Додати `config.py` та `.env` в `.gitignore`
- ✅ Регулярно оновлювати токени
- ✅ Обмежувати права доступу бота

### Приклад безпечного налаштування

```python
# config.py
import os
from dotenv import load_dotenv

load_dotenv()

TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")
THREADS_ACCESS_TOKEN = os.getenv("THREADS_ACCESS_TOKEN")
# ...
```

```bash
# .env (НЕ додавайте в Git!)
TELEGRAM_BOT_TOKEN=your_token
THREADS_ACCESS_TOKEN=your_token
```

## 📈 Масштабування

### Один канал → один акаунт
```
Telegram канал → Bot → Threads акаунт
```

### Кілька каналів → один акаунт
```python
# Модифікуйте bot.py для прослуховування кількох каналів
TELEGRAM_CHANNELS = ["@channel1", "@channel2", "@channel3"]
```

### Один канал → кілька акаунтів
```python
# Створіть кілька інстансів ThreadsAPI
threads_accounts = [
    ThreadsAPI(user_id1, token1),
    ThreadsAPI(user_id2, token2),
]

for api in threads_accounts:
    api.create_and_publish_text(text)
```

## 🐛 Відлагодження

### Проблема: Бот не бачить пости

**Діагностика:**
```bash
# Перевірте, що бот має права
python -c "from telegram import Bot; import asyncio; bot = Bot('YOUR_TOKEN'); asyncio.run(bot.get_me())"
```

**Рішення:**
1. Бот має бути адміністратором каналу
2. У бота мають бути права на читання

### Проблема: Помилка Threads API

**Діагностика:**
```bash
# Перевірте токен
curl "https://graph.threads.net/v1.0/me?access_token=YOUR_TOKEN"
```

**Рішення:**
1. Перевірте правильність токена
2. Оновіть токен, якщо прострочений
3. Перевірте дозволи (permissions)

### Проблема: GPT не розбиває текст

**Діагностика:**
```python
from text_splitter import TextSplitter
splitter = TextSplitter("YOUR_KEY", "gpt-4o-mini")
result = splitter.split_text("Ваш довгий текст...")
print(result)
```

**Рішення:**
1. Перевірте баланс OpenAI
2. Перевірте API ключ
3. Спробуйте іншу модель

## 📊 Статистика та аналітика

### Вбудована аналітика

Бот логує:
- ✅ Кількість оброблених постів
- ✅ Час обробки кожного поста
- ✅ Успішні/невдалі публікації
- ✅ Використання OpenAI (кількість розбивок)

### Розширення аналітикою

Додайте в `bot.py`:

```python
import json
from datetime import datetime

# Статистика
stats = {
    "total_posts": 0,
    "successful": 0,
    "failed": 0,
    "text_splits": 0,
    "media_posts": 0
}

def save_stats():
    with open("stats.json", "w") as f:
        json.dump(stats, f)

# Викликайте після кожної публікації
stats["total_posts"] += 1
stats["successful"] += 1
save_stats()
```

## 🔄 Оновлення

### Оновлення коду

```bash
git pull origin main
pip install -r requirements.txt --upgrade
python test_connection.py
sudo systemctl restart threads-bot
```

### Оновлення залежностей

```bash
pip install --upgrade python-telegram-bot openai requests
```

### Міграція даних

При оновленні `config.py`:
1. Збережіть старі токени
2. Оновіть структуру
3. Протестуйте через `test_connection.py`

## 🌟 Можливі розширення

### 1. Фільтрація контенту
Додайте фільтри для вибіркової публікації:
```python
if message.text.startswith("#publish"):
    # Публікувати
```

### 2. Розклад публікацій
Відкладайте публікації на певний час:
```python
from datetime import datetime, timedelta
publish_time = datetime.now() + timedelta(hours=2)
```

### 3. Мультимовність
Автоматичний переклад постів:
```python
from googletrans import Translator
translator = Translator()
translated = translator.translate(text, dest='en')
```

### 4. Аналітика
Інтеграція з Google Analytics або власна база даних:
```python
import sqlite3
# Зберігайте статистику
```

### 5. Вебхуки замість polling
Для швидшої реакції:
```python
# Використовуйте webhooks замість polling
application.run_webhook(...)
```

## 📚 Документація API

### Threads API Limits

| Параметр | Ліміт |
|----------|-------|
| Пости на день (користувач) | 250 |
| Пости на день (додаток) | 1000 |
| Символів в пості | 500 |
| Елементів в каруселі | 2-10 |
| Розмір фото | 8 MB |
| Розмір відео | 1 GB |
| Тривалість відео | 5 хв |
| Rate limit | ~200/год |

### Telegram Bot API

| Параметр | Ліміт |
|----------|-------|
| Повідомлень на секунду | 30 |
| Файл | 50 MB |
| Фото (найбільше) | 10 MB |

## 🆘 Підтримка

### Де шукати допомогу

1. **Документація проєкту:**
   - `README.md` — основна інформація
   - `SETUP_GUIDE.md` — детальне налаштування
   - `API_EXAMPLES.md` — приклади API

2. **Офіційна документація:**
   - [Threads API](https://developers.facebook.com/docs/threads)
   - [Telegram Bot API](https://core.telegram.org/bots/api)
   - [OpenAI API](https://platform.openai.com/docs)

3. **Логи:**
   - Перевірте `threads_bot.log`
   - Запустіть з `LOG_LEVEL="DEBUG"`

## 📝 Ліцензія

Цей проєкт створений для особистого використання.

## 🙏 Подяки

- Meta за Threads API
- Telegram за Bot API
- OpenAI за GPT API
- Python community за чудові бібліотеки

---

**Створено з ❤️ для автоматизації контент-маркетингу**

*Версія: 1.0.0*  
*Дата: Січень 2025*

