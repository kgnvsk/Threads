# 🌐 Розгортання Threads Bot на сервері 24/7

## 🎯 Чому потрібен сервер?
- 💻 Локальний комп'ютер може вимикатися/засинати
- 🔋 Сервер працює 24/7 без перерв
- 🌍 Доступ з будь-де
- 💪 Стабільне інтернет-з'єднання

---

## 💰 Варіанти хостингу (від дешевого до дорогого)

### 1️⃣ **AWS EC2 Free Tier** (БЕЗКОШТОВНО 12 місяців) ⭐️ РЕКОМЕНДУЮ
**Ціна:** $0 перший рік, потім ~$5-10/міс  
**Характеристики:** 750 годин/міс, 1GB RAM, 1 CPU  

✅ **Переваги:**
- Безкоштовно перший рік
- Надійно
- Легко масштабувати

❌ **Недоліки:**
- Потрібна кредитна карта
- Трохи складніше налаштування

📖 **Інструкція:** [👇 Дивись нижче](#aws-ec2-безкоштовно)

---

### 2️⃣ **Hetzner Cloud** (НАЙДЕШЕВШЕ)
**Ціна:** €4.15/міс (~$4.5)  
**Характеристики:** 2GB RAM, 1 CPU, 20GB SSD  

✅ **Переваги:**
- Дуже дешево
- Швидко
- Просте налаштування
- Серверне розташування в Європі

❌ **Недоліки:**
- Потрібна оплата

📖 **Інструкція:** [👇 Дивись нижче](#hetzner-cloud)

---

### 3️⃣ **DigitalOcean** (ПОПУЛЯРНО)
**Ціна:** $6/міс (перші 2 місяці $4)  
**Характеристики:** 1GB RAM, 1 CPU, 25GB SSD  

✅ **Переваги:**
- Проста панель управління
- Багато туторіалів
- Швидке розгортання

❌ **Недоліки:**
- Трохи дорожче за Hetzner

📖 **Інструкція:** [👇 Дивись нижче](#digitalocean)

---

### 4️⃣ **Google Cloud Free Tier** (БЕЗКОШТОВНО)
**Ціна:** $0 (назавжди для e2-micro)  
**Характеристики:** 0.25-1GB RAM, 1 CPU  

✅ **Переваги:**
- Безкоштовно назавжди
- Надійно

❌ **Недоліки:**
- Мало RAM (може бути повільно)
- Складне налаштування

📖 **Інструкція:** [👇 Дивись нижче](#google-cloud)

---

### 5️⃣ **Oracle Cloud Free Tier** (БЕЗКОШТОВНО)
**Ціна:** $0 (назавжди)  
**Характеристики:** 1GB RAM або ARM з 24GB RAM  

✅ **Переваги:**
- Безкоштовно назавжди
- Багато ресурсів (ARM)

❌ **Недоліки:**
- Складна реєстрація
- Може блокувати акаунти

---

## 🚀 Інструкції по розгортанню

---

## AWS EC2 (БЕЗКОШТОВНО)

### Крок 1: Створення сервера
```bash
1. Зайди на https://aws.amazon.com/free/
2. Зареєструйся (потрібна картка, але не списують)
3. Перейди в EC2 Dashboard
4. Натисни "Launch Instance"
5. Вибери:
   - OS: Ubuntu Server 22.04 LTS
   - Instance type: t2.micro (Free tier eligible)
   - Key pair: Створи нову SSH ключ
   - Storage: 8GB (достатньо)
6. Launch!
```

### Крок 2: Підключення до сервера
```bash
# Завантаж SSH ключ (file.pem)
chmod 400 your-key.pem

# Підключись
ssh -i "your-key.pem" ubuntu@your-ec2-ip.compute.amazonaws.com
```

### Крок 3: Налаштування сервера
```bash
# Оновлення системи
sudo apt update && sudo apt upgrade -y

# Встановлення Python та залежностей
sudo apt install python3 python3-pip git -y

# Клонування проекту (або завантаж файли)
mkdir ~/Threads
cd ~/Threads

# Завантаж всі файли бота (використай scp або git)
```

### Крок 4: Встановлення залежностей
```bash
pip3 install python-telegram-bot openai requests
```

### Крок 5: Налаштування автозапуску
```bash
# Скопіюй service файл
sudo cp threads-bot.service /etc/systemd/system/

# Відредагуй шляхи в service файлі
sudo nano /etc/systemd/system/threads-bot.service
# Зміни /Users/admin/Threads на /home/ubuntu/Threads
# Зміни User=admin на User=ubuntu

# Активуй
sudo systemctl daemon-reload
sudo systemctl enable threads-bot
sudo systemctl start threads-bot

# Перевір
sudo systemctl status threads-bot
```

---

## Hetzner Cloud

### Крок 1: Створення сервера
```bash
1. Зайди на https://www.hetzner.com/cloud
2. Зареєструйся та додай оплату
3. Створи новий проект
4. "Add Server":
   - Location: Європа (найближча)
   - Image: Ubuntu 22.04
   - Type: CX11 (€4.15/міс)
   - SSH Key: Додай свій публічний ключ
5. Create & Buy
```

### Крок 2: Підключення
```bash
ssh root@your-server-ip
```

### Крок 3-5: Те саме що AWS ⬆️

---

## DigitalOcean

### Крок 1: Створення Droplet
```bash
1. Зайди на https://www.digitalocean.com/
2. Create → Droplets
3. Вибери:
   - Ubuntu 22.04 LTS
   - Basic Plan: $6/mo
   - Datacenter: Найближчий
   - SSH Key: Додай свій
4. Create Droplet
```

### Крок 2-5: Те саме що AWS ⬆️

---

## Google Cloud

### Крок 1: Створення VM
```bash
1. https://console.cloud.google.com/
2. Compute Engine → VM Instances
3. Create Instance:
   - Machine type: e2-micro (Free tier)
   - Boot disk: Ubuntu 22.04 LTS
   - Firewall: Allow HTTP/HTTPS
4. Create
```

### Крок 2: Підключення
```bash
# Використай вбудований SSH в браузері
# Або налаштуй SSH ключ
```

### Крок 3-5: Те саме що AWS ⬆️

---

## 📦 Швидке розгортання (універсальний скрипт)

Після підключення до сервера:

```bash
# 1. Оновлення системи
sudo apt update && sudo apt upgrade -y

# 2. Встановлення Python
sudo apt install python3 python3-pip git -y

# 3. Створення директорії
mkdir -p ~/Threads
cd ~/Threads

# 4. Завантаження файлів (використай один з варіантів):

# Варіант А: scp з локального комп'ютера
# На ЛОКАЛЬНОМУ комп'ютері:
# scp -r /Users/admin/Threads/* user@server-ip:~/Threads/

# Варіант Б: git (якщо є репозиторій)
# git clone your-repo-url .

# 5. Встановлення залежностей
pip3 install python-telegram-bot openai requests

# 6. Тест запуску
python3 bot.py
# Ctrl+C для зупинки

# 7. Налаштування systemd
sudo nano /etc/systemd/system/threads-bot.service
```

**Вміст threads-bot.service для серверу:**
```ini
[Unit]
Description=Threads Bot
After=network.target

[Service]
Type=simple
User=YOUR_USERNAME
WorkingDirectory=/home/YOUR_USERNAME/Threads
ExecStart=/usr/bin/python3 /home/YOUR_USERNAME/Threads/bot.py
Restart=always
RestartSec=10
StandardOutput=append:/home/YOUR_USERNAME/Threads/threads_bot.log
StandardError=append:/home/YOUR_USERNAME/Threads/threads_bot.log

[Install]
WantedBy=multi-user.target
```

```bash
# 8. Активація
sudo systemctl daemon-reload
sudo systemctl enable threads-bot
sudo systemctl start threads-bot

# 9. Перевірка
sudo systemctl status threads-bot
tail -f ~/Threads/threads_bot.log
```

---

## 🔧 Корисні команди на сервері

```bash
# Статус бота
sudo systemctl status threads-bot

# Перезапуск
sudo systemctl restart threads-bot

# Логи
tail -f ~/Threads/threads_bot.log

# Останні 100 рядків логів
tail -100 ~/Threads/threads_bot.log

# Тільки помилки
grep ERROR ~/Threads/threads_bot.log

# Зупинка
sudo systemctl stop threads-bot

# Оновлення бота (після змін)
cd ~/Threads
# Завантаж нові файли
sudo systemctl restart threads-bot
```

---

## 🔐 Безпека сервера

### 1. Налаштуй firewall:
```bash
sudo ufw allow 22/tcp  # SSH
sudo ufw enable
sudo ufw status
```

### 2. Зміни SSH порт (опціонально):
```bash
sudo nano /etc/ssh/sshd_config
# Зміни Port 22 на інший (наприклад 2222)
sudo systemctl restart ssh
```

### 3. Вимкни root логін:
```bash
sudo nano /etc/ssh/sshd_config
# PermitRootLogin no
sudo systemctl restart ssh
```

### 4. Налаштуй автооновлення:
```bash
sudo apt install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades
```

---

## 📊 Моніторинг

### Перевірка ресурсів:
```bash
# CPU та RAM
htop

# Дисковий простір
df -h

# Процеси Python
ps aux | grep python
```

### Налаштування алертів (опціонально):
```bash
# Встанови uptimerobot.com (безкоштовно)
# Він буде пінгувати твій сервер кожні 5 хв
```

---

## 🆘 Troubleshooting

### Бот не запускається:
```bash
# Перевір логи
sudo journalctl -u threads-bot -n 50

# Перевір права
ls -la ~/Threads/bot.py

# Перевір Python
which python3
python3 --version
```

### Бот падає:
```bash
# Перевір RAM
free -h

# Якщо мало RAM, додай swap:
sudo fallocate -l 1G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### Conflict errors:
```bash
# Зупини всі процеси
pkill -9 python3
sudo systemctl restart threads-bot
```

---

## 💡 Поради

1. **Вибери найближчий datacenter** - швидше з'єднання
2. **Використовуй SSH ключі**, не паролі
3. **Регулярно роби бекапи** `bot_data.db`
4. **Моніторь логи** перші дні
5. **Налаштуй алерти** на падіння бота

---

## 📞 Моя рекомендація

**Для початку:** AWS EC2 Free Tier (безкоштовно перший рік)  
**Довгостроково:** Hetzner Cloud (€4.15/міс, найдешевше)  
**Якщо потрібно безкоштовно назавжди:** Google Cloud або Oracle Cloud

---

## 🎯 Швидкий чеклист

- [ ] Створив сервер
- [ ] Підключився через SSH
- [ ] Оновив систему
- [ ] Встановив Python та залежності
- [ ] Завантажив файли бота
- [ ] Налаштував config.py
- [ ] Протестував запуск
- [ ] Налаштував systemd
- [ ] Перевірив автозапуск (reboot)
- [ ] Налаштував firewall
- [ ] Налаштував моніторинг

**Готово! Бот працює 24/7** 🎉

