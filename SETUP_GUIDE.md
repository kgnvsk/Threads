# 📖 Детальний посібник з налаштування Threads Bot

## 🔐 Крок 1: Отримання Threads API доступу

### 1.1. Створення Meta App

1. Перейдіть на [Facebook Developers](https://developers.facebook.com/)
2. Натисніть **"Create App"** (Створити додаток)
3. Виберіть use case: **"Threads"**
4. Заповніть базову інформацію про додаток
5. Створіть додаток

### 1.2. Налаштування Threads Use Case

1. У Dashboard вашого додатку знайдіть розділ **"Use Cases"**
2. Виберіть **"Threads"** та натисніть **"Customize"**
3. Налаштуйте необхідні дозволи (permissions)

### 1.3. Отримання Access Token

**Необхідні дозволи (Permissions):**
- ✅ `threads_basic` — базовий доступ до API
- ✅ `threads_content_publish` — публікація контенту
- ✅ `threads_manage_replies` — керування відповідями
- ✅ `threads_read_replies` — читання відповідей

**Як отримати токен:**

#### Варіант А: Через Graph API Explorer (для тестування)

1. Перейдіть на [Graph API Explorer](https://developers.facebook.com/tools/explorer/)
2. Виберіть ваш додаток у верхньому правому куті
3. Натисніть **"Generate Access Token"**
4. Виберіть необхідні дозволи (permissions)
5. Скопіюйте згенерований токен

#### Варіант Б: Через Authorization Flow (для production)

Згідно з документацією: https://developers.facebook.com/docs/threads/get-started

1. **Authorization Window** - надайте користувачу можливість авторизуватись
2. **Authorization Code** - отримайте код авторизації
3. **Exchange for Access Token** - обміняйте код на токен

**Приклад отримання Long-Lived Token:**

```bash
# Короткостроковий токен діє 1 годину
# Long-lived token діє 60 днів

curl -X GET \
  "https://graph.threads.net/access_token?grant_type=th_exchange_token&client_id=YOUR_APP_ID&client_secret=YOUR_APP_SECRET&access_token=SHORT_LIVED_TOKEN"
```

**Відповідь:**
```json
{
  "access_token": "YOUR_LONG_LIVED_TOKEN",
  "token_type": "bearer",
  "expires_in": 5184000
}
```

### 1.4. Отримання Threads User ID

**Спосіб 1: Через API запит**

```bash
curl -X GET \
  "https://graph.threads.net/v1.0/me?fields=id,username&access_token=YOUR_ACCESS_TOKEN"
```

**Відповідь:**
```json
{
  "id": "1234567890123456",
  "username": "your_username"
}
```

**Спосіб 2: Через Graph API Explorer**

1. У Graph API Explorer введіть `/me?fields=id,username`
2. Натисніть **"Submit"**
3. Скопіюйте `id` з відповіді

---

## 🤖 Крок 2: Налаштування Telegram Bot

### 2.1. Створення бота

1. Відкрийте Telegram
2. Знайдіть [@BotFather](https://t.me/BotFather)
3. Відправте команду `/newbot`
4. Введіть ім'я бота (наприклад: "Threads Publisher")
5. Введіть username бота (має закінчуватись на `bot`, наприклад: `threads_publisher_bot`)
6. Скопіюйте отриманий **Bot Token**

### 2.2. Додавання бота до каналу

1. Відкрийте ваш Telegram канал
2. Перейдіть в налаштування каналу
3. Виберіть **"Адміністратори"** → **"Додати адміністратора"**
4. Знайдіть вашого бота
5. Надайте боту права:
   - ✅ **Читання повідомлень** (обов'язково!)
   - Інші права на ваш розсуд

### 2.3. Отримання Channel ID

**Для публічних каналів:**
```
@your_channel_username
```

**Для приватних каналів:**

1. Перешліть будь-яке повідомлення з каналу боту [@userinfobot](https://t.me/userinfobot)
2. Він покаже вам Chat ID (наприклад: `-1001234567890`)

Або використовуйте цей скрипт:

```python
# get_channel_id.py
from telegram import Bot
import asyncio

async def get_updates():
    bot = Bot(token="YOUR_BOT_TOKEN")
    updates = await bot.get_updates()
    for update in updates:
        print(update)

asyncio.run(get_updates())
```

---

## 🔑 Крок 3: Отримання OpenAI API Key

### 3.1. Реєстрація на OpenAI

1. Перейдіть на [OpenAI Platform](https://platform.openai.com/)
2. Зареєструйтесь або увійдіть в акаунт
3. Додайте баланс (мінімум $5)

### 3.2. Створення API ключа

1. Перейдіть в розділ [API Keys](https://platform.openai.com/api-keys)
2. Натисніть **"Create new secret key"**
3. Введіть назву ключа (наприклад: "Threads Bot")
4. Скопіюйте згенерований ключ (він показується тільки один раз!)

### 3.3. Вибір моделі

**Рекомендовані моделі:**
- `gpt-4o-mini` — швидка та дешева ($0.15 за 1M токенів)
- `gpt-4o` — якісніша, але дорожча ($2.50 за 1M токенів)
- `gpt-4-turbo` — альтернатива

**Для більшості випадків `gpt-4o-mini` цілком достатньо!**

---

## ⚙️ Крок 4: Налаштування бота

### 4.1. Редагування config.py

Відкрийте файл `config.py` та заповніть свої дані:

```python
@dataclass
class Config:
    # Telegram налаштування
    TELEGRAM_BOT_TOKEN: str = "7907738818:AAG2Y6eXE2xspWG_aXFi50gGCkt6pveohAM"
    TELEGRAM_CHANNEL_ID: str = "@kgnvsk_ai"  # або -1001234567890
    
    # Threads API налаштування
    THREADS_USER_ID: str = "27645323711725380"
    THREADS_ACCESS_TOKEN: str = "THQWJVWnB4MWp5djNCUXhvRXUyWmlZA..."
    
    # OpenAI налаштування
    OPENAI_API_KEY: str = "sk-proj-abc123..."
    OPENAI_MODEL: str = "gpt-4o-mini"
    
    # Інші налаштування (можна не змінювати)
    MAX_TEXT_LENGTH: int = 450
    PUBLISH_DELAY: int = 3
    STATUS_CHECK_DELAY: int = 5
    MAX_STATUS_CHECKS: int = 20
```

### 4.2. Альтернатива: Використання змінних середовища

Створіть файл `.env`:

```bash
# .env
TELEGRAM_BOT_TOKEN=7907738818:AAG2Y6eXE2xspWG_aXFi50gGCkt6pveohAM
TELEGRAM_CHANNEL_ID=@kgnvsk_ai
THREADS_USER_ID=27645323711725380
THREADS_ACCESS_TOKEN=THQWJVWnB4MWp5djNCUXhvRXUyWmlZA...
OPENAI_API_KEY=sk-proj-abc123...
```

Потім завантажте:

```python
from dotenv import load_dotenv
load_dotenv()
```

---

## 🧪 Крок 5: Тестування налаштувань

### 5.1. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 5.2. Запуск тестів

```bash
python test_connection.py
```

**Що перевіряє скрипт:**
- ✅ Правильність Telegram токена та ID каналу
- ✅ Правильність OpenAI API ключа
- ✅ Підключення до Threads API
- ✅ Створення тестового поста (без публікації)
- ✅ Перевірку статусу поста

**Очікуваний результат:**
```
============================================================
✅ ВСІ ТЕСТИ ПРОЙДЕНО УСПІШНО!
============================================================

📝 Примітка: Тестовий пост НЕ був опубліковано в Threads.
   Він створений, але залишився в статусі чернетки.

🚀 Ви можете запускати бота: python bot.py
============================================================
```

---

## 🚀 Крок 6: Запуск бота

### 6.1. Простий запуск

```bash
python bot.py
```

### 6.2. Запуск у фоновому режимі (Linux/Mac)

```bash
nohup python bot.py > bot_output.log 2>&1 &
```

### 6.3. Запуск через systemd (Linux)

Створіть файл `/etc/systemd/system/threads-bot.service`:

```ini
[Unit]
Description=Threads Auto Publisher Bot
After=network.target

[Service]
Type=simple
User=YOUR_USERNAME
WorkingDirectory=/path/to/Threads
ExecStart=/usr/bin/python3 /path/to/Threads/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустіть:

```bash
sudo systemctl daemon-reload
sudo systemctl enable threads-bot
sudo systemctl start threads-bot
sudo systemctl status threads-bot
```

### 6.4. Запуск через Docker

Створіть `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

Зберіть та запустіть:

```bash
docker build -t threads-bot .
docker run -d --name threads-bot --restart unless-stopped threads-bot
```

---

## 📊 Крок 7: Моніторинг

### 7.1. Перегляд логів

```bash
tail -f threads_bot.log
```

### 7.2. Що бот логує

```
2025-01-14 12:05:30 - __main__ - INFO - Отримано новий пост з каналу: 12345
2025-01-14 12:05:30 - __main__ - INFO - Обробляємо текстовий пост (567 символів)
2025-01-14 12:05:30 - __main__ - INFO - Текст довгий, розбиваємо на частини
2025-01-14 12:05:35 - text_splitter - INFO - Текст розбито на 3 частин
2025-01-14 12:05:35 - threads_api - INFO - Текстовий пост створено: 1234567890
2025-01-14 12:05:40 - threads_api - INFO - Пост опубліковано: 9876543210
2025-01-14 12:05:45 - threads_api - INFO - Частина 2 опубліковано: 1111111111
2025-01-14 12:05:50 - threads_api - INFO - Частина 3 опубліковано: 2222222222
2025-01-14 12:05:50 - __main__ - INFO - ✅ Пост успішно опубліковано (3 частин)
```

---

## 🔧 Розширені налаштування

### Зміна максимальної довжини тексту

```python
MAX_TEXT_LENGTH: int = 400  # замість 450
```

### Збільшення затримок для відео

```python
PUBLISH_DELAY: int = 5  # замість 3
STATUS_CHECK_DELAY: int = 10  # замість 5
MAX_STATUS_CHECKS: int = 30  # замість 20
```

### Використання GPT-4 для кращої якості

```python
OPENAI_MODEL: str = "gpt-4o"  # замість gpt-4o-mini
```

---

## ❓ FAQ

### Бот не бачить пости з каналу

**Проблема:** Бот не реагує на нові пости

**Рішення:**
1. Переконайтесь, що бот доданий як **адміністратор** каналу
2. Перевірте, що у бота є права на **читання повідомлень**
3. Для приватних каналів використовуйте числовий ID (наприклад: `-1001234567890`)
4. Перезапустіть бота

### Invalid access token

**Проблема:** Помилка при спробі публікації в Threads

**Рішення:**
1. Перевірте, що токен скопійовано повністю (без пробілів)
2. Переконайтесь, що токен не прострочений (long-lived токени діють 60 днів)
3. Оновіть токен через Graph API Explorer
4. Перевірте, що токен має всі необхідні дозволи

### Медіа не публікуються

**Проблема:** Фото/відео не з'являються в Threads

**Рішення:**
1. Telegram file URLs мають бути доступні публічно
2. Збільште `STATUS_CHECK_DELAY` для відео (вони обробляються довше)
3. Перевірте логи на наявність конкретних помилок
4. Переконайтесь, що медіа відповідає специфікаціям Threads

### OpenAI повертає помилку

**Проблема:** Довгі тексти не розбиваються

**Рішення:**
1. Перевірте баланс на OpenAI Platform
2. Переконайтесь, що API ключ правильний
3. Спробуйте іншу модель (`gpt-4o-mini` → `gpt-4o`)
4. Перевірте логи для деталей помилки

---

## 📚 Корисні посилання

### Офіційна документація

- [Threads API Documentation](https://developers.facebook.com/docs/threads)
- [Threads API Reference](https://developers.facebook.com/docs/threads/reference)
- [Publishing to Threads](https://developers.facebook.com/docs/threads/posts)
- [Get Started with Threads API](https://developers.facebook.com/docs/threads/get-started)
- [Postman Collection](https://www.postman.com/meta/threads/overview)

### Інструменти

- [Graph API Explorer](https://developers.facebook.com/tools/explorer/)
- [Meta for Developers](https://developers.facebook.com/)
- [OpenAI Platform](https://platform.openai.com/)

### Python бібліотеки

- [python-telegram-bot](https://docs.python-telegram-bot.org/)
- [OpenAI Python SDK](https://github.com/openai/openai-python)
- [Requests](https://requests.readthedocs.io/)

---

## 🎯 Підсумок

Після виконання всіх кроків у вас буде:

✅ Telegram бот, підключений до вашого каналу  
✅ Автоматична публікація всіх постів з каналу в Threads  
✅ Розумна розбивка довгих текстів на ланцюжки  
✅ Підтримка фото, відео та медіа-груп  
✅ Детальне логування та обробка помилок  

**Готово! Тепер просто публікуйте пости у ваш Telegram канал, і вони автоматично з'являться в Threads! 🚀**

